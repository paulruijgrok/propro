"""Interfaces to external platforms/tools.

- ``propro.interfaces.pdf_reports`` — render propro results as standalone PDF
  documents (e.g. a one-page sequence overview).
- ``propro.interfaces.gscripts``    — push/pull propro results to/from Google
  Sheets.
- ``propro.interfaces.excel``       — Excel (VBA) glue for propro results.
- ``propro.interfaces.pymol``       — drive PyMOL sessions from propro results
  (e.g. color a structure by net charge or hydrophobicity).
"""
